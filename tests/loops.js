(function () {
  meta('Using for loops solve the exercises below')
  let nums = [1,2,5]
  let sum;
  it('sum should be 8', () => {
    expect(sum).toBe(8)
  })

  let str = 'cool'
  let str2;
  it('make a backwards string', () => {
    expect(str2).toBe('looc')
  })
}())